# array construction
# list
names = ['ahmed','mohamed','hassan']
# array
numbers = [2,8,9,6,7,25]
# matrice
numbers2 = [2,8,7],[8,6,13],[14,18,24]
# matrice opperations append, remove, insert, pop, index
names.append('hussam')
# names.remove('ahmed')

# extraction from the array max, min, len
d = max(numbers)
dd = min(numbers)
ddd = len(numbers)

# printing the different variables and indexing through arrays
print(d,dd,ddd)
print(names[0],'    ',numbers[0:3], '   ', numbers2[1])